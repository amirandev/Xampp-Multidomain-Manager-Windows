<?xml version="1.0" encoding="utf-8" ?>
<!--
  Powered By XROW.ASIA
  Real Life Soultions for IT World
  Contact: amoswaper@gmail.com
-->

<Page
    x:Class="XamppMultidomainManager.Pages.PhpIniPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12" Padding="32,32,32,16">
            <TextBlock Text="php.ini" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center" />
            <Button x:Name="ReloadBtn" Click="ReloadBtn_Click" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE72C;" FontSize="12" />
                    <TextBlock Text="Reload" />
                </StackPanel>
            </Button>
            <Button x:Name="SaveBtn" Click="SaveBtn_Click" Style="{StaticResource AccentButtonStyle}" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE74E;" FontSize="12" />
                    <TextBlock Text="Save" />
                </StackPanel>
            </Button>
        </StackPanel>

        <ScrollViewer Grid.Row="1" Margin="32,0,32,16" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <TextBox x:Name="Editor"
                     FontFamily="Cascadia Code, Consolas"
                     AcceptsReturn="True"
                     TextWrapping="NoWrap"
                     IsSpellCheckEnabled="False"
                     IsTextPredictionEnabled="False"
                     BorderThickness="1"
                     Padding="8"
                     MinHeight="400" />
        </ScrollViewer>

        <TextBlock x:Name="StatusBar" Grid.Row="2" Padding="32,8,32,12"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   Style="{StaticResource CaptionTextBlockStyle}" />
    </Grid>
</Page>
