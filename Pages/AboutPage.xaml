<?xml version="1.0" encoding="utf-8" ?>
<!--
  Powered By XROW.ASIA
  Real Life Soultions for IT World
  Contact: amoswaper@gmail.com
-->

<Page
    x:Class="XamppMultidomainManager.Pages.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <StackPanel Spacing="16" Padding="32">
            <TextBlock Text="About" Style="{StaticResource TitleTextBlockStyle}" />

            <Border Style="{StaticResource CardStyle}" MaxWidth="600">
                <StackPanel Spacing="12">
                    <TextBlock Text="Xampp Multidomain Manager" Style="{StaticResource SubtitleTextBlockStyle}" FontWeight="SemiBold" />
                    <TextBlock Text="Version 1.0.0" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock TextWrapping="WrapWholeWords">
                        A Windows desktop application that provides a graphical interface for managing multiple local domains (virtual hosts) on a XAMPP installation. No more manually editing Apache config files, hosts file, or MySQL databases — everything is handled through a clean WinUI 3 dashboard.
                    </TextBlock>
                    <HyperlinkButton Content="GitHub" NavigateUri="https://github.com/amirandev/Xampp-Multidomain-Manager-Windows" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}" MaxWidth="600">
                <StackPanel Spacing="12">
                    <TextBlock Text="Requirements" Style="{StaticResource SubtitleTextBlockStyle}" FontWeight="SemiBold" />
                    <TextBlock TextWrapping="WrapWholeWords">
                        • Windows 10 (version 1809 or later) or Windows 11&#x0a;
                        • XAMPP latest version installed at C:\xampp (Apache + MySQL)&#x0a;
                        • .NET SDK 10.0 (for development/debugging)
                    </TextBlock>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}" MaxWidth="600">
                <StackPanel Spacing="12">
                    <TextBlock Text="Tech Stack" Style="{StaticResource SubtitleTextBlockStyle}" FontWeight="SemiBold" />
                    <TextBlock TextWrapping="WrapWholeWords">
                        • UI Framework: WinUI 3 (Windows App SDK 2.2)&#x0a;
                        • Language: C# with .NET 10&#x0a;
                        • Architecture: MVVM pattern&#x0a;
                        • Local Database: SQLite via Microsoft.Data.Sqlite&#x0a;
                        • SSL: OpenSSL (bundled with XAMPP)&#x0a;
                        • Packaging: MSIX (Windows App SDK)
                    </TextBlock>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}" MaxWidth="600">
                <StackPanel Spacing="8">
                    <TextBlock Text="Powered By" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="XROW.ASIA" FontSize="20" FontWeight="Bold" />
                    <TextBlock Text="Real Life Solutions for IT World" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <HyperlinkButton Content="amoswaper@gmail.com" NavigateUri="mailto:amoswaper@gmail.com" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
